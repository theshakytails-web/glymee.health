name = "glymee-api"
main = "worker/index.ts"
compatibility_date = "2024-12-01"

# Bindings
[vars]
ENVIRONMENT = "production"

# Secrets (set via wrangler secret put):
# MAILCHANNELS_API_KEY — from Cloudflare Dashboard → MailChannels
# MAILCHANNELS_ACCOUNT_ID — your Cloudflare Account ID
